# Pre-journey production archive (2026-07-09)

Snapshot of the production SPA shell before promoting the journey UI to `/`.

## Current production

Light shell + journey product flow is the only build (`npm run build` → `client/dist`).  
This folder is a **recovery snapshot** only — do not restore into the live tree without an explicit request.
