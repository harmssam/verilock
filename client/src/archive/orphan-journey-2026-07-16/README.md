# Orphan `src/journey/` components (2026-07-16)

Unused component copies that lived under `client/src/journey/` while production product UI lived in `experiment/` (later renamed to `journey/`).

These were **excluded from TypeScript** (`tsconfig.app.json`) and had **zero imports** from the live app.

Archived so the `journey/` path can hold the real product modules without name collision.
